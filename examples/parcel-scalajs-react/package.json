{
  "name": "parcel-scalajs-react",
  "source": "index.html",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "start": "parcel",
    "build": "parcel build",
    "postinstall": "scalawind generate -sr -pcr -o ./src/scalawind.scala",
    "dev:scala": "scala-cli --cli-version 1.3.2 --power package . -w -f -o scalajs-modules",
    "build:scala": "scala-cli --cli-version 1.3.2 --power package . -f -o scalajs-modules"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.19",
    "parcel": "^2.12.0",
    "postcss": "^8.4.38",
    "scalawind": "^0.7.3",
    "tailwindcss": "^3.4.4"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
