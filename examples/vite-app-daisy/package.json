{
  "name": "scalawind-vite",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "postinstall": "scalawind generate --check-duplication --check-optimization -f laminar -o ./src/scalawind.scala",
    "dev:scala": "scala-cli --cli-version 1.6.2 --power package . -w -f -o scalajs-modules",
    "build:scala": "scala-cli --cli-version 1.6.2 --power package . -f -o scalajs-modules"
  },
  "devDependencies": {
    "autoprefixer": "10.4.19",
    "postcss": "8.4.38",
    "scalawind": "latest",
    "daisyui": "4.12.2",
    "tailwindcss": "3.4.3",
    "vite": "5.2.11"
  }
}
