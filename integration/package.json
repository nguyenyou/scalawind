{
  "name": "integration-test",
  "type": "module",
  "scripts": {
    "generate": "scalawind generate -pcr -o ./src/scalawind.scala",
    "test:scala": "scala-cli --cli-version 1.3.2 test src"
  },
  "dependencies": {
    "scalawind": "link:scalawind"
  }
}