{
  "name": "integration-test",
  "type": "module",
  "scripts": {
    "generate": "scalawind generate --check-duplication --check-optimization --framework laminar -pcr -o ./src/scalawind.scala",
    "test:scala": "scala-cli test src --test-only 'tests.only*'"
  },
  "dependencies": {
    "scalawind": "link:scalawind"
  }
}